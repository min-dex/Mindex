const fs=require('fs'),assert=require('assert/strict');const {chromium}=require('/Users/parkjihun/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');
(async()=>{const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});try{const p=await b.newPage();await p.route('**/*supabase*/**',r=>r.abort());await p.route('**/app.js*',r=>r.fulfill({path:require('path').resolve(__dirname,'../app.js'),contentType:'application/javascript'}));await p.goto('https://studexx.github.io/Mindex/',{waitUntil:'domcontentloaded'});await p.waitForFunction(()=>typeof independentServiceContentTitle==='function');const result=await p.evaluate(()=>{const service={id:'test',type_id:'friday',date:'2026-09-18'};state.services=[service];state.selectedServiceId='test';state.loadedWorshipServiceIds.add('test');const out=[];for(const [label,type,title,assignee] of [['자율기도','title','',''],['묵도','title','',''],['새 순서','title','',''],['대표기도','title_person','','담당 집사'],['대표기도','title_person','',''],['설교 제목','title_person','','김남영 목사'],['설교 제목','title_person','새 말씀','김남영 목사'],['새 순서','title','다른 제목','']]){const memo={...emptyServiceItemMemo(),elementType:type,inputMode:'text'};const item={id:label,service_id:'test',label,raw_title:title,assignee,memo:serializeServiceItemMemo(memo)};out.push({label,type,title,assignee,status:resolvePresenterServiceItemContentState(item,memo,null,service),model:serviceItemEditorModel(item,{service}).showTitle,save:serviceElementTitleForSave({...item,raw_title:title||label},type),fixed:presenterFixedTitleText(item)});}return out;});assert.equal(result[0].status.state,'filled');assert.equal(result[1].status.state,'filled');assert.equal(result[2].status.state,'filled');assert.equal(result[3].status.state,'filled');assert.equal(result[4].status.state,'missing');assert.equal(result[5].status.state,'missing');assert.equal(result[6].status.state,'filled');assert.equal(result[7].fixed,'다른 제목');assert.equal(result[0].save,'');assert.equal(result[3].save,'');assert.equal(result[6].save,'새 말씀');console.log('PASS generic label title, required person, required sermon title, custom title, save normalization');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});
