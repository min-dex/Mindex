# 청년부 주보

## 저장 구조

예배 한 건에 주보 한 건을 연결한다. 별도 발행본·템플릿 버전 테이블은 만들지 않는다.

`mindex_bulletins`의 데이터는 두 묶음이다.

- `content`: 주보 문구, 일정·위원표 표시 월, 월별 일정, 공통 내용 재사용 정보, 저장 당시 자동 채움 값, 인쇄용 순서 정리 선택.
- `layout`: 자동 배경 모드 또는 공용 배경 파일명, 프레임별 위치·크기·글자 크기·정렬·표시 여부.

`service_id`로 예배를 참조하고, `revision`과 `updated_at`은 저장 충돌 확인용이다.
예배 순서·찬양·본문·대표기도·교회 일정은 기존 DB에서 읽는다.
주보 저장이 예배나 찬양 원본을 변경하지 않는다.

## 배포 상태와 적용 순서

2026-09-23 사용자가 운영 DB에 테이블 추가 SQL을 적용했다. 필수 열의 REST 읽기와
실제 청년부 예배의 편집창·2쪽 미리보기·공용 배경 목록 연결을 확인했다.

1. [테이블 추가 SQL](../migrations/2026-09-23-bulletin-content-layout.sql)을 운영 DB에 적용한다.
2. 테이블·권한·revision 트리거를 확인한다.
3. 해당 클라이언트를 배포한다. 테이블 없이 배포하면 편집창은 DB 연결 실패를 표시한다.

쓰기 역할은 기존 공유 Worship 클라이언트와 동일한 `anon`, `authenticated`다.
관리자 인증 강화 스크립트 적용 시 다른 관리 테이블과 함께 authenticated 쓰기로 제한한다.
클라이언트는 서비스 키를 사용하지 않는다.

## 저장·복구

- 저장 버튼과 Ctrl/Cmd+S는 내용과 양식을 한 번에 DB에 저장한다.
- 읽은 revision이 현재 DB와 같을 때만 수정한다. 동시 생성·수정 충돌은 덮어쓰지 않는다.
- 입력 중 내용은 브라우저에 복구본으로 남긴다. DB 저장 성공과 구분해서 상태를 표시한다.
- 기존 로컬 초안과 날짜별 공통 문구 설정은 삭제하지 않는다.
- DB에 주보가 없으면 기존 로컬 초안을 가져와 저장할 수 있다.
- DB 주보가 있으면 DB를 먼저 연다. 기존 브라우저 초안은 별도 복구 버튼으로 불러온다.
- 충돌 후 DB 다시 불러오기 전에 현재 수정본을 복구본으로 남긴다.
- 이미지 데이터는 주보 DB에 복제하지 않는다. 기기에서만 등록한 배경은 다른 기기에서도
  같은 이름으로 등록되어 있어야 하며, 없으면 안내하고 출력을 막는다.

## 원본과 사용자 선택

- 배경은 기존 MINDEX 배경 목록의 실제 등록/기본 자산 전체를 사용한다.
  여섯 개만 골라둔 목록·별칭은 제거했다.
- 신규 주보는 날짜·부서에 따른 자동 배경으로 시작한다. Presenter의 기존 배경 선택 함수를 공유하며 절기 배경을 우선한다. 자동 모드는 DB에도 그대로 저장한다.
- 기존 초안에서 직접 선택한 배경과 배경 없음은 보존한다. 양식의 배경에서 자동으로 전환할 수 있다.
- 신규 옵션에서 임의의 흰 바탕·짙은 테두리 테마를 제거했다. 기존 선택값은 복구를 위해 읽을 수 있다.
- 일부 배경에만 적용하던 흰 패널 88% 불투명도 규칙을 제거했다.
- 광고의 명시적인 환영·정기 안내 문단을 나누고, 원문은 별도로 보존한다. 인식하지 못한 문구는 소식에 남긴다.
- 새 주보는 인쇄용 순서 정리를 기본 적용한다. 기존 저장본의 명시적인 선택은 유지한다.
- 날짜별 실주보 공통 문구를 자동 적용한다. 직접 저장한 문구와 의도적인 빈 값은 보존한다.
- 발행 호수는 확인된 과거 날짜만 사용하고 미래 값을 임의 계산하지 않는다.
- 좌표 2.5mm, 글자 크기 2.5pt 단계와 A4 접지선 148.5mm는 수치 정돈 요청에 따라 유지한다.

## 출력·검증

A4 가로 두 페이지를 SVG로 렌더링하고 메모리 내 인쇄 iframe을 사용한다.
원본 폰트 Freesentation과 기존 배경 자산을 사용한다. 영역 넘침, 원본/자산 읽기 실패 시
인쇄를 막는다. 출력에는 편집 UI가 들어가지 않는다.

검증 파일:

- `tests/test_bulletin.cjs`: 원문 보존, 순서 정리 선택, 내용/양식 직렬화, 날짜별 참고 문구.
- `tests/test_bulletin_persistence.cjs`: 실제 DB adapter의 revision 조건, 충돌·권한·스키마 오류.
- `tests/smoke_bulletin.cjs`: 편집·저장·충돌·복구·브라우저 초기화 후 DB 복원·공용 배경·2쪽 PDF.

원본 조사 근거는 [72개 주보 조사](young-adult-bulletin-archive-review.md)를 참고한다.
운영 DB에 테스트 데이터를 쓰지는 않았다. 충돌·저장 실패·브라우저 초기화 후 복원은
오프라인 통합 검사로 검증했다.


## 2026-09-24 코드 점검

- 복구본 저장 실패는 DB 저장 오류와 분리한다. 용량 부족/접근 차단 시에도 DB 읽기와 저장을 계속한다.
- DB 다시 불러오기는 메모리에 현재 수정본을 보존한다. 브라우저 저장 실패가 다시 불러오기를 중단하지 않는다.
- 복구본은 허용된 내용/설정/프레임 값만 읽고 DB revision과 예배 ID는 복구본에서 가져오지 않는다.
- 복구본에 기록한 시각으로 최신 초안을 선택한다. 손상된 JSON이나 프레임 구조는 DB 로딩을 막지 않는다.
- 본문형 예배 항목은 제목뿐 아니라 저장된 body를 읽는다. 순서 간추리기를 끄면 고백 전문이 유지된다.
- 안쪽 교회명도 입력한 교회명 데이터를 사용한다. 별도 고정 문자열을 제거했다.
- 출력에서 숨긴 영역의 넘침은 인쇄를 막지 않는다. 출력할 영역의 가로/세로 넘침과 너무 좁은 열은 차단한다.
- 인쇄는 한 번에 하나만 준비하고, 편집창이 닫히면 인쇄 준비를 취소한다.

브라우저 통합 검사에서 QuotaExceededError와 SecurityError, 손상/오래된 복구본,
DB 저장 실패/충돌, 숨긴 영역, 좁은 열, 중복 인쇄와 인쇄 중 창 닫기를 재현했다.
운영 DB에는 점검용 데이터를 쓰지 않았다. 사용자의 실제 브라우저에서 발생한 저장소 오류 종류는 확인하지 못했다.

## 독립 주보 섹션

왼쪽 주보 메뉴는 예배와 동등한 독립 모듈(`bulletin`)이다. 메뉴·예배 화면의 주보 버튼·날짜 선택은 모두 현재 페이지 탭에서 화면을 전환한다. 주보 진입 과정에서는 새 탭을 만들거나 다른 탭으로 이동하지 않는다. 새 탭은 사용자가 명시적으로 추가할 때만 생성한다.

청년부 주보의 날짜 목록에서 편집기를 열며 탭 제목은 `청년부 주보`로 표시한다. 연결된 예배 ID로 원본을 읽고 주보 문구·양식은 기존 주보 DB에 따로 저장한다. 날짜별 미저장 내용과 실행 취소 기록은 해당 페이지 탭의 문서 메모리에 유지한다. 주보 전용 돌아가기 버튼은 없으며 다른 화면으로 이동할 때 공통 메뉴를 사용한다. 이전 Presenter 주보 주소도 독립 주보 모듈로 연다.

## 2026-09-26: 실주보의 변경 주기에 따른 내용 연결

2026년 실주보 26개의 월간 일정 패널을 다시 추출했고, 같은 월에는 내용이 동일함을 확인했다.
8/2·8/16·8/23과 9/6·9/13·9/20의 소식·위원표를 비교해 월간 목록과 주간 변경을 구분했다.
2025-01-26/02-02의 표어 변경, 11/30/12/07의 임원 변경도 다시 확인했다.
기존 72개 자료 조사와 함께 사용하며, 반복 관찰을 미래 행사 확정으로 간주하지 않는다.

| 변경 주기 | 항목 | 자동 입력 출처 | 사용자가 할 일 |
| --- | --- | --- | --- |
| 장기 | 교회명·웹사이트·주소·예배 시간/장소 | 적용 날짜에 맞는 실주보 문구 → 이전 주보에서 명시적으로 저장한 공통 수정 | 바뀔 때 공통 내용에서 한 번 수정·저장 |
| 장기 | 표어·성구·섬김이·환영·정기 안내 | 같은 공통 내용 규칙. 연초 자동 교체 없음 | 실제 개편/시간 변경 때 수정. 해당 호 안내만 생략하려면 프레임 숨김 |
| 월간 | 교회 일정, 날짜·요일·시간 | 같은 달의 저장된 일정 → 확인된 2026년 실주보 일정 → 교회력 DB | 자료가 없는 달의 금요 월삭 등 주일 교회력에 없는 일정·시간만 월 1회 보완 |
| 월간 목록 + 주간 갱신 | 예배위원표 | `mindex_sunday_calendar.young_adult_prayer` + 예배의 휴회/연합 상태 | 교회력에서 배정 입력/변경. 주보에 재입력하지 않음 |
| 주간 | 날짜·교회력·기념주일 | 예배 날짜 + 교회력 `liturgical`, `note` | 원본 데이터 오류가 있을 때 원본에서 수정 |
| 주간 | 인도자·찬양·대표기도·본문·설교·결단·봉헌·파송·담당자 | 저장된 예배 aggregate + 연결된 찬양/성구 | 예배에서 한 번 입력. 인도자 누락은 주보에서도 보완 가능 |
| 주간 | 청년부 소식 | 해당 예배의 광고 body | 광고에 없는 추가 소식, 주보용 줄임 문구만 편집 |
| 주간·선택 | 설교 요점 | 현재 저장 예배 모델에는 요점 필드가 없음 | 전달받은 요점만 주보에 입력. 없으면 공란 유지 |
| 발행 시 | 호수 | 확인된 과거 발행 파일의 번호 | 새 발행분 번호 확인·입력. 예배 수/달력 주차로 추측하지 않음 |
| 계절·특별 주일 | 배경 | 기존 MINDEX 날짜·부서·절기 배경 선택 | 예외 디자인일 때만 양식에서 변경 |

### 재사용과 보존

- 별도 테이블/스키마 추가 없이 `content.reuse.common`과 `content.reuse.months[YYYY-MM]`를 사용한다.
  직접 수정한 장기/월간 내용만 공유한다. 매주 소식·인도자·설교 요점·호수는 절대 전주에서 복사하지 않는다.
- 같은 부서의 현재 날짜 이전 저장 주보를 날짜순으로 합친다. 현재 주보 자체와 미래 날짜 주보는 제외한다.
  한 달 안에서도 수정본이 생기면 그 날짜 이후 새 주보에서 최신 수정본을 사용한다.
- 2026년 1~9월의 일정은 각각 해당 월 실주보에 실린 날짜/시각/명칭 그대로 초기 자료로 제공한다.
  미래 월삭·찬양예배를 첫 금요일/셋째 주일 규칙으로 생성하지 않는다.
- 월간 일정은 지난 날짜도 유지한다. 이는 이전의 '오늘 이후만' 규칙을 사용자의 실주보 기준 요청에 따라 대체한다.
- `content.months`는 현재 문서에서 편집한 각 월의 일정을 보관한다. 표시 월을 왕복해도 편집 내용이 남는다.
- `content.inherited`는 저장 당시 자동 연결된 공통/월간 문구를 보관한다. 기존 저장본은 나중 공통 수정으로 몰래 바뀌지 않는다.
  '예배 자료 갱신'은 자동 채움 값을 다시 연결하며 직접 편집한 값은 유지한다. 공통 내용의 '다시 연결'은 직접 편집 값도 해제하며 실행 취소할 수 있다.
- 현재 예배·교회력 연결 자체는 갱신 시 다시 읽는다. 이 변경은 완전한 발행본 보관 기능을 추가한 것은 아니다.
- 2025의 월말 위원표 다음 달 전환, 사진 표지 등은 현행 2026 양식에 자동 이식하지 않는다.

검증: 월 경계, 적용 시작일, 미래 수정 배제, 전주 소식 미복사, 수동 빈 값, 저장본 유지,
부서/날짜별 DB 조회, 월 선택 왕복, 실제 9/20 주보의 자동 채움 및 양면 출력.
