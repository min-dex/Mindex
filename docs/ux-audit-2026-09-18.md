# UX Audit: 2026-09-18

## Scope

UX/controller work only. Reviewed repository README/HANDOFF, documentation map,
UX and presenter contracts, shared EX practices, runtime entry points, and
synthetic browser tests. No schema changes, production writes, real projector
launches, or broad staging of the shared working tree. No root AGENTS.md exists.

## Findings And Fixes

- P2: citation submission forced controller scrolling away from the live input.
  Removed that submission-only scroll; ordinary slide navigation is unchanged.
- P2: viewport restoration anchored the element header, so adding slides above
  the new bottom composer moved the input. Anchor the focused composer instead,
  including checkbox/button focus. Synthetic Chrome/WebKit reproduction changed
  from 200px drift to 0px.
- Documentation: the thread guide's undated Recent Updates mixed historical
  topbar/linked-song behavior with current instructions. Preserved the notes as
  historical and linked current contracts. Added the citation contract to the
  documentation map and recorded the new behavior in the presenter decision log.

## Consistency And Data Boundaries

- Retained shared shell tokens, 34px composer input/action height, Korean labels,
  accessible icon labels, and the user's explicit static multicolor border.
- Runtime source scan found no dummy/fixture/mock/sample seed markers in app.js,
  mindex.presenter.js, mindex.presenter-ui.js, or index.html. This is not a
  production-database audit and does not prove that all stored data is clean.
- Keep data/emergency-worship-2026-08-09.json: dated recovery material connected
  to the emergency snapshot loader, not an identified dummy seed.
- Keep data/scripture-books.csv and tests: reference/import material and regression
  fixtures, not deletion candidates. No operational data was deleted.
- No obsolete Documents path or conflicting localhost port found in the searched
  documentation. README/HANDOFF port 2300 matches serve.py. Relative Markdown
  links were checked across 24 documents; two cross-repository links resolve in
  the real sibling workspace, though not in the isolated release worktree.
- Shared README/HANDOFF and data-owner documents were read but not rewritten.
  Historical data incidents and migration notes remain intact.

## Verification

Passed: global search (40 queries plus Enter/IME/stale input), DB tabs and return
navigation, header save draft retention including failure/retry, input polish,
presenter number navigation, inline preparation examples, citation scroll,
citation output navigation, live controller stability, and composer layout.
Relevant tests include Chrome and WebKit; fixtures block Supabase requests and
stub persistence/output actions. JS syntax and diff whitespace checks also run
before release.

## Remaining Limits

No production DB dummy audit, physical multi-monitor/fullscreen check, Electron
release, actual projector playback, or new timing benchmark. Existing behavior
tests do not establish full-app defect freedom or transactional save guarantees.
Unrelated local boot/font changes from another task are excluded from this release.
